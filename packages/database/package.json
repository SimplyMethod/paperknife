{
  "name": "@paperknife/database",
  "version": "0.0.1",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "db:schema:dump": "prisma migrate diff --from-empty --to-schema-datamodel schema.prisma --script > baseline.sql"
  },
  "dependencies": {
    "@libsql/client": "^0.3.5",
    "@prisma/adapter-libsql": "^5.4.2",
    "@prisma/client": "^5.4.2"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.1.0",
    "dotenv": "^16.3.1",
    "prisma": "^5.4.2",
    "tsconfig": "workspace:*",
    "typescript": "^5.2.2"
  },
  "author": ""
}
